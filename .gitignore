# secrets
.hf_token
*.token
.env
.env.*

# large model weights — ship via a GitHub Release, not git
*.safetensors
*.bin
*.pt
classifier/code/models/model_c_distilbert/

# caches / bytecode / regenerable
*.npz
.cache/
__pycache__/
*.pyc
.ipynb_checkpoints/

# raw & heavy data (regenerated by the scripts)
*.jsonl
*.jsonl.gz
*.parquet
hustle*.csv
yt_metadata_en*
